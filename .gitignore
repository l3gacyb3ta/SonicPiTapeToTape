node_modules/
dist/
.DS_Store
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Vercel project config
.vercel/

# Dev artifacts (moved to ~/.anvideck/projects/sonicPiWeb/)
artifacts/*
!artifacts/README.md

# Planning artifacts (internal)
.planning/

# Playwright test results
test-results/

# Parity viewers mirror .captures/ artifacts (~1 GB of WAVs per full sweep).
# CHECKED IN (small, regenerable from manifests): the HTML viewers
# index.html / examples-sweep.html / book-examples-sweep.html /
# launch-gate.html / fx-inspector.html and the JSON manifests
# (examples-sweep.json, book-examples-sweep.json, e2e.json, community.json).
# IGNORED: the per-slug WAV/PNG artifact dirs and the two large SSR viewers
# (e2e.html, community.html) — all regenerate on demand via the build-* tools.
test_results/fx/
test_results/fx-baseline.json
# Dashboard inline-audio runtime (generated by `npm run dashboard:audio`): the
# same-origin engine bundle + tree-sitter wasm + frozen-PRNG wavs. The committed
# loader is test_results/audio-controls.js; these heavy binaries regenerate from
# src/engine + public/.
test_results/spw-engine.mjs
test_results/spw-engine.mjs.map
test_results/tree-sitter.wasm
test_results/tree-sitter-ruby.wasm
test_results/rand-stream*.wav
test_results/e2e/
test_results/e2e.html
test_results/community/
test_results/community.html
test_results/examples-sweep/
test_results/book-examples-sweep/
test_results/raw-lpf/
# SP107 mono-sample investigation captures (≈10 MB WAVs + PNGs). The viewer
# mono-sample-sp107.html is checked in; the recordings regenerate via
# tools/compare-desktop-vs-web.ts. See the page footer for the commands.
test_results/sp107/
# Launch-gate detail page assets (≈44 MB desktop+web WAVs + spectrograms for the
# 7 gate rows). The viewer gate-detail.html is checked in; assets regenerate via
# tools/build-gate-detail.ts after a sweep.
test_results/gate-detail/
# Experiments investigation dashboard assets (det_*/norand_*/numbeats_* WAVs +
# PNGs + event/compare JSON). The viewer experiments.html is checked in; the
# artifacts regenerate via tools/compare-desktop-vs-web.ts + event-parity.ts.
test_results/experiments/

# Playwright
playwright-report/

# Editor
.vscode/
.idea/
.vercel

# Browser diagnostic captures (internal observation tool)
.captures/
tmp/

# Personal learnings (not part of the project)
LEARNINGS.md
tests/book-examples/

# Generated dashboard publish bundle (regenerated from test_results/)
dashboard-dist/

# VitePress docs (generated)
docs/.vitepress/cache/
docs/.vitepress/dist/
docs/public/rand-stream.wav
