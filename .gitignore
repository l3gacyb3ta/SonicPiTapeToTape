node_modules/
dist/
.DS_Store
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# Vercel project config
.vercel/

# Dev artifacts (moved to ~/.anvideck/projects/sonicPiWeb/)
artifacts/*
!artifacts/README.md

# Planning artifacts (internal)
.planning/

# Playwright test results
test-results/

# FX A/B inspector — mirrors .captures/ artifacts (≈80 MB of WAVs per build)
# Built by `npm run inspect`. The HTML viewers (test_results/index.html and
# test_results/e2e.html) are the only checked-in viewer files; everything
# else is regenerated on demand. raw-lpf/ is per-investigation captures.
test_results/fx/
test_results/fx-baseline.json
test_results/e2e/
test_results/e2e.html
test_results/community/
test_results/community.html
test_results/raw-lpf/
# SP107 mono-sample investigation captures (≈10 MB WAVs + PNGs). The viewer
# mono-sample-sp107.html is checked in; the recordings regenerate via
# tools/compare-desktop-vs-web.ts. See the page footer for the commands.
test_results/sp107/

# Playwright
playwright-report/

# Editor
.vscode/
.idea/
.vercel

# Browser diagnostic captures (internal observation tool)
.captures/
tmp/

# Personal learnings (not part of the project)
LEARNINGS.md
tests/book-examples/
